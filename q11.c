#include <stdio.h>

int main() {
    int n, i, evenCount = 0, oddCount = 0;
    printf("Enter number of scores: ");
    scanf("%d", &n);

    int arr[n], even[n], odd[n];
    printf("Enter scores: ");
    for
